pre-commit:
  commands:
    lint:
      glob: "{src,static,test}/**/*.{js,mjs}"
      run: npx eslint {staged_files}

commit-msg:
  commands:
    conventional:
      run: npx commitlint --edit {1}

pre-push:
  commands:
    test:
      run: node scripts/pre-push-test.js
